<!-- define the content for the template variable "content" -->
{{#partial "content"}}
  {{#if username }}
    <h1>Welcome {{ username }}!</h1>
  {{ else }}
    <h1>Welcome to the Courses Page</h1>

    <form action="/sign-in" method="post">
      <input type="text" placeholder="What's your name?" name="username">
      <button type="submit">Sign In</button>
    </form>
  {{/if}}
{{/partial}}

<!-- use the base.hbs template -->
{{> base.hbs}}
